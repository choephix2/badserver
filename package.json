{
  "name": "badserver",
  "version": "0.0.1",
  "description": "Configurable server, to deliberately return chosen mock and error responses.",
  "main": "serve.js",
  "scripts": {
    "start": "node serve.js --port 1118 --config ./config.json"
  },
  "author": "cx",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "yargs": "^15.0.2"
  }
}
